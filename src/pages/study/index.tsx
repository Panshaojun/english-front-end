import './index.scss';
import { observer } from 'mobx-react';
import Opt from './components/Opt';
const Study = () => {
    return (
        <>
        <Opt/>
        </>
    )
}
export default observer(Study);